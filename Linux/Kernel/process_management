Process Management in Operating System:

1. What is a Process?
A process is a program in execution.

A program is a passive entity (set of instructions), whereas a process is an active entity (with program counter, stack, data, etc.). A process is the basic unit of CPU scheduling and resource management.
------------------------------------
2. Components of a Process
Every process consists of:

Text Section (Code) → Instructions to be executed.
Data Section → Global variables.
Heap → Dynamically allocated memory during runtime.
Stack → Function calls, local variables, return addresses.
Program Counter (PC) → Address of next instruction.

Process Control Block (PCB) → Metadata of process (explained below).
---------------------------------
3. Process Control Block (PCB)
Each process is represented by a PCB in the OS.
The PCB stores all essential information about a process:

Process ID (PID)
Process State (running, waiting, etc.)
Program Counter
CPU Registers
Memory Management Info (base, limit, page tables)
Scheduling Info (priority, scheduling queues)
I/O Status Info (open files, devices)

PCB acts as the identity card of a process.
------------------------------------
4. Process States
A process moves through multiple states during execution:

New → Process is being created.
Ready → Waiting for CPU allocation.
Running → Currently executing on CPU.
Waiting/Blocked → Waiting for I/O or event.
Terminated → Finished execution.

Some systems also add: Suspended Ready and Suspended Blocked.

State Transition Diagram is often used to show movement between states.
----------------------------------
5. Process Scheduling
Since CPU is limited, the OS uses scheduling to decide which process runs next.

Types of Schedulers:
Long-term Scheduler (Job Scheduler):Decides which jobs enter the system.
Controls degree of multiprogramming.

Medium-term Scheduler:Suspends/resumes processes.Improves performance by reducing load.

Short-term Scheduler (CPU Scheduler):Decides which ready process runs on CPU.Runs frequently (milliseconds).
-------------------------------------
6. Scheduling Queues
Processes are managed using queues:

Job Queue → All processes in the system.
Ready Queue → All processes ready for CPU.
Device Queue → Processes waiting for I/O devices.
-----------------☆☆☆☆
7. Context Switching
When CPU switches from one process to another:

Current process state is saved in its PCB.
New process state is loaded from its PCB.
Context switching is overhead (no useful work, but essential for multitasking).
----------☆☆☆☆☆☆☆☆☆☆☆☆☆-
8. Types of Process
Independent Process:Does not affect or depend on others.

Cooperating Process:Can affect or be affected by others.

Need for Inter-Process Communication (IPC).
---------------------------------
9. Inter-Process Communication (IPC)
Processes need to communicate to share data.

IPC Methods:
Shared Memory → Processes share a memory region. Fast, but needs synchronization.

Message Passing → Processes exchange messages (send/receive). Safer, but slower.

IPC is crucial for client-server systems, parallel computing, and distributed systems.
------------------------------------
10. Process Synchronization
When multiple processes access shared resources, synchronization is needed to avoid conflicts.

Critical Section Problem → Section of code accessing shared resource.

Synchronization Tools:
Semaphores
Mutex
Monitors
Spinlocks
----------------------------------
11. Threads vs Processes
Process → Heavyweight, has own memory space.

Thread → Lightweight, shares process resources.

Multi-threading improves CPU utilization but needs synchronization.
----------------------------------
12. Deadlock in Process Management
Deadlock occurs when a set of processes are waiting indefinitely for resources held by each other.

Conditions for deadlock (Coffman’s Conditions):

Mutual Exclusion
Hold and Wait
No Preemption
Circular Wait
-----------------------------------
13. Process Scheduling Algorithms
Some important CPU scheduling algorithms:

FCFS (First Come First Serve) – Non-preemptive, simple queue.

SJF (Shortest Job First) – Preemptive/non-preemptive, optimal but hard to predict.

Priority Scheduling – Based on priority levels.

Round Robin – Time-sharing, fair for all.

Multilevel Queue – Separate queues for types of processes.

Multilevel Feedback Queue – Dynamic movement between queues.
----------------------------------
14. Advantages of Process Management
Efficient CPU utilization.

Supports multiprogramming & multitasking.

Provides resource sharing.

Ensures process isolation & protection.

Handles concurrency & synchronization.

---------------------------------------

Here are the main types of processes:

🔹 1. Based on Execution
Foreground Process → Runs in direct interaction with the user (e.g., terminal command, GUI app).

Background Process → Runs without user interaction (e.g., daemons, services).

🔹 2. Based on Function
System Process → Managed by the OS, handles internal system tasks (e.g., init, systemd).

User Process → Created and run by users (e.g., browsers, compilers).

🔹 3. Based on Behavior
I/O-Bound Process → Spends more time waiting for I/O (disk, network) than using CPU.

CPU-Bound Process → Spends more time using CPU (calculations, data processing).

🔹 4. Based on Creation
Parent Process → The one that creates another process.

Child Process → Created by a parent using fork() or similar system call.

🔹 5. Based on Communication
Independent Process → Does not share data with others.

Cooperating Process → Shares data/resources, uses IPC (shared memory, pipes, sockets).

🔹 6. Based on Threading
Single-threaded Process → One thread of execution.

Multi-threaded Process → Multiple threads sharing same memory and resources.

------------------------------------

⚙️ Process Execution Models in Operating Systems
Operating systems manage how multiple programs or tasks execute on a CPU.
These models define how CPU time and system resources are shared.

🔹 1. Multiprogramming
Meaning: Multiple programs are loaded into memory; CPU switches between them when one waits (usually for I/O).

Goal: Maximize CPU utilization.

Example: In batch systems, while one job waits for disk I/O, another uses the CPU.

Analogy: While your washing machine runs, you start cooking — CPU never idle.

🔹 2. Multitasking
Meaning: Multiple tasks (programs or processes) run apparently at the same time by fast switching.

Goal: Improve responsiveness for users.

Example: Listening to music while writing code and downloading files.

Note: It’s a form of multiprogramming with time-sharing.

🔹 3. Multiprocessing
Meaning: A system with two or more CPUs (cores) executes multiple processes in true parallelism.

Goal: Boost performance and reliability.

Example: Multi-core processors running multiple apps simultaneously.

🔹 4. Multithreading
Meaning: A single process is divided into multiple threads that run concurrently, sharing memory.

Goal: Perform multiple subtasks simultaneously within one application.

Example: A web browser – one thread for rendering, one for input, one for network.

Benefit: Faster execution, less memory usage than multiple processes.

🔹 5. Distributed Processing
Meaning: Computation is split across multiple networked systems working together.

Goal: Scalability, fault tolerance, and performance.

Example: Hadoop cluster, Kubernetes, or microservices on cloud nodes.

🔹 6. Time-Sharing
Meaning: Each process gets a fixed CPU time slice, then the CPU switches to another process.

Goal: Fair CPU allocation among multiple users or processes.

Example: Multi-user Unix/Linux systems.

Note: Basis for multitasking in modern OSes.

🔹 7. Real-Time Processing
Meaning: System guarantees completion of tasks within strict time constraints.

Goal: Predictability and reliability in time-sensitive operations.

Example: Airbag system, flight control, pacemakers.

Types:

Hard real-time: Missing a deadline = system failure.

Soft real-time: Occasional misses acceptable.

🔹 8. Concurrency
Meaning: Managing multiple tasks at once, not necessarily executing simultaneously.

Goal: Structure systems to handle many overlapping operations.

Example: Handling thousands of simultaneous HTTP requests via async I/O.

🔹 9. Parallelism
Meaning: Actually running multiple tasks at the same time using multiple CPUs/cores.

Goal: Speed up computation.

Example: Matrix multiplication on GPU cores.

Note:

Parallelism ⇒ Concurrency

But Concurrency ≠ Parallelism (can exist on single core).
-------------------------------------
